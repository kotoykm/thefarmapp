<p style="color: green"><%= notice %></p>

<h1>Granjeros</h1>

<div id="farmers">
  <% @farmers.each do |farmer| %>
    <%= farmer.name %>
    <p>
      <%= link_to "Mostrar", farmer %>
    </p>
  <% end %>
</div>

<%= link_to "Nuevo Granjero", new_farmer_path, class: "btn btn-warning text-white fw-bold" %>
