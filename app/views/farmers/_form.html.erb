<%= form_with(model: farmer) do |form| %>
  <% if farmer.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(farmer.errors.count, "error") %> prohibited this farmer from being saved:</h2>

      <ul>
        <% farmer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name, class: "form-control w-25"%>
  </div>

  <div class="mb-3">
    <%= form.label :age, style: "display: block" %>
    <%= form.text_field :age, class: "form-control w-25"%>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
